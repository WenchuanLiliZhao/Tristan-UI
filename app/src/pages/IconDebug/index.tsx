import type { DemoPageType } from "..";
import { Element } from "./_Element";

const IconDebug: DemoPageType = {
  name: "Icon Debug",
  path: "icon-debug",
  content: <Element />
}

export default IconDebug; 